// gör så att funktionerna validerar om en användare har tillgång baserat på
// ålder - 18 eller äldre
// är prenumerant
// är inte bannad

export const checkAccess = (age, hasSubscription, isBanned) => {};

// bonusövning - testa att skriva samma validering på en rad
// (testet kommer inte kunna se detta utan kontrollerar bara värdet den får tillbaka)
// lägg till export framför const (som på rad 6) för att även testa denna funktion
const checkAccessOneLine = (age, hasSubscription, isBanned) => {};
